# Dark Scribble Cat Configuration
# This file configures the Dark Scribble bongo cat pack

name=Dark Scribble Cat
bodyImage=body-darkscribblecat.png
handUpImage=handup-darkscribblecat2.png
handDownImage=handdown-darkscribblecat.png
iconImage=body-darkscribblecat.png

# UI ordering weight (higher appears earlier in UI)
weight=99.9

# Body offsets (relative to base position)
bodyOffsetX=0.0
bodyOffsetY=10.0

# Left arm offsets
leftArmOffsetX=0.0
leftArmOffsetY=0.0
leftArmSpacing=1.1

# Right arm offsets
rightArmOffsetX=50.0
rightArmOffsetY=0.0
rightArmSpacing=2.2

# Punch animation offset (multiplier for how high arms move)
punchOffsetY=0.1

# Hand down texture vertical offset (adjusts position when switching to hand down texture)
handDownOffsetY=33.0
