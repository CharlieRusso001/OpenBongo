# DevArt Cat Configuration
# This file configures the DevArt bongo cat pack

name=Googley Dev Art Cat
bodyImage=body-devartcat.png
handUpImage=handup-devartcat.png
handDownImage=handdown-devartcat.png
iconImage=body-devartcat.png

# UI ordering weight (higher appears earlier in UI)
weight=99

# Body offsets (relative to base position)
bodyOffsetX=0.0
bodyOffsetY=0.0

# Left arm offsets
leftArmOffsetX=0.0
leftArmOffsetY=0.0
leftArmSpacing=1.1

# Right arm offsets
rightArmOffsetX=40.0
rightArmOffsetY=0.0
rightArmSpacing=1.0

# Punch animation offset (multiplier for how high arms move)
punchOffsetY=0.3

# Hand down texture vertical offset (adjusts position when switching to hand down texture)
handDownOffsetY=40.0

