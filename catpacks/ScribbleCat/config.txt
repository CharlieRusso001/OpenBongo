# DevArt Cat Configuration
# This file configures the DevArt bongo cat pack

name=Scribble Cat
bodyImage=body-scribblecat.png
handUpImage=handup-scribblecat2.png
handDownImage=handdown-scribblecat.png
iconImage=body-scribblecat.png

# UI ordering weight (higher appears earlier in UI)
weight=100

# Body offsets (relative to base position)
bodyOffsetX=0.0
bodyOffsetY=10.0

# Left arm offsets
leftArmOffsetX=0.0
leftArmOffsetY=0.0
leftArmSpacing=1.1

# Right arm offsets
rightArmOffsetX=50.0
rightArmOffsetY=0.0
rightArmSpacing=2.2

# Punch animation offset (multiplier for how high arms move)
punchOffsetY=0.1

# Hand down texture vertical offset (adjusts position when switching to hand down texture)
handDownOffsetY=33.0
